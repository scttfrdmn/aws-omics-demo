# Measurements — NOT YET RUN

This file will hold the measured numbers as each lifecycle phase runs, with the
raw per-leg JSON beside it (`arm64-n<N>-fsx.json`, `x86-n<N>-fsx.json`,
`variant_qc.json`). **It is currently empty of data — the pipeline has not been run.**

Do not add numbers here by hand. They get written from actual runs via
`benchmark/lifecycle_metrics.py` and `benchmark/analyze_study.py`. See
[`../../STATUS.md`](../../STATUS.md) for the run procedure and
[`../../../docs/results.md`](../../../docs/results.md) for the templates describing
what will be recorded (end-to-end lifecycle, per-stage instance-lifetime timing,
and variant-QC validation: Ti/Tv, variant counts, population differentiation).

## Run log

_(empty — first run appends here)_
